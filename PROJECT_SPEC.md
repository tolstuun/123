# VMRay Analytics Platform Specification

The platform continuously preserves normalized short-lived VMRay analysis results and provides authenticated round-aware detection, duration, VTI, search, export, and collector-health analytics. Every source run remains independently identifiable by its VMRay provenance.

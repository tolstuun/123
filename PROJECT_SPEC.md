# VMRay Analytics Platform Specification

The platform continuously preserves short-lived VMRay analysis results and provides authenticated long-term verdict, VTI, IOC, completeness, comparison, search, export, and collector-health analytics. Every source run and raw response remains independently recoverable. Real and demo observations are explicitly separated.
